<table class="mq-table pure-table-bordered pure-table">
    <thead>
        <tr>
            <th class="highlight">Key</th>
            <th class="highlight">CSS Media Query</th>
            <th>Applies</th>
            <th>Classname</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td class="highlight"><i>None</i></td>
            <td class="highlight"><i>None</i></td>
            <td><i>Always</i></td>
            <td>{{code ".pure-u-*"}}</td>
        </tr>

      {{#each selectedUnits}}
        <tr>
            <td class="highlight"><b>{{code @key}}</b></td>
            <td class="mq-table-mq highlight"><code>@media {{this.mq}}</code></td>
            <td>≥ <b>{{this.expressions.0.value}}</b></td>
            <td><code>.pure-u-<b>{{@key}}</b>-*</code></td>
        </tr>
      {{/each}}
    </tbody>
</table>
